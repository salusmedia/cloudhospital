# 应用场景登记表

> 新建场景前必须在此登记并与架构组确认关联关系。一个编号 = 一对前后端目录。
> 编号一旦废弃不再复用。

| 编号 | 场景名称 | 负责人 | 依赖的平台能力 | 对外暴露 API 前缀 | 状态 | 备注 |
|------|----------|--------|----------------|-------------------|------|------|
| 001 | <示例：在线随访> | <姓名> | auth, patient | /api/scenario-001 | 开发中 | 示例行，可删 |
| 002 | | | | | 规划中 | |
| 003 | | | | | 规划中 | |
| 019 | 转诊一件事 | owner-019 | auth, patient | /api/scenario-019 | 开发中 | 脚手架生成 |
| 006 | 线上复诊 | owner-006 | auth, patient | /api/scenario-006 | 开发中 | 脚手架生成 |
| 002 | 家庭病床 | owner-002 | auth, patient | /api/scenario-002 | 开发中 | 脚手架生成 |

**状态取值**：规划中 / 开发中 / 联调中 / 已上线 / 已废弃

**关联关系说明**（场景之间如何协作，举例）：
- 场景 A 需要患者数据 → 调 `platform-patient`，不自存。
- 场景 B 需要场景 A 的结果 → 通过场景 A 暴露的 `/api/scenario-A/...` HTTP 接口（经网关），不直接 import。
- 共享的类型放 `packages/shared-types`，共享的组件放 `packages/ui`。
