# 应用场景登记表

> 新建场景前必须在此登记并与架构组确认关联关系。一个编号 = 一对前后端目录。
> 编号一旦废弃不再复用。

| 编号 | 场景名称 | 依赖的平台能力 | 对外暴露 API 前缀 | 状态 | 备注 |
|------|----------|----------------|-------------------|------|------|
| 001 | 在线随访 | auth, patient, py-common | /api/scenario-001 | 联调中 | 后端真实 DB（scenario_followup schema）；独立前端已完成（随访计划/记录/按日期筛选/新增 + 单测） |
| 002 | 家庭病床 | auth, patient, iot, clearing, py-common | /api/scenario-002 | 联调中 | 后端完整；独立前端已建（apps/scenario-002-frontend） |
| 006 | 在线复诊 | auth, patient, platform-ai, clearing, py-common | /api/scenario-006 | 联调中 | 后端完整；AI 审方走 platform-ai；独立前端已建 |
| 019 | 转诊一件事 | auth, patient, clearing, py-common | /api/scenario-019 | 联调中 | 后端最完整（转诊/分账/MDT/信用）；前端已建 |

**已注销 / 空占位**：编号 003 未开发，原 002 重复行已合并。

**状态取值**：规划中 / 开发中 / 联调中 / 已上线 / 已废弃

**关联关系说明**：
- 所有场景的患者数据 → 调 `platform-patient`，不自存。
- 场景 006（开方）→ 调 `platform-ai/rx-review` 做 AI 处方审核（同容器 localhost:8103）。
- 场景 019（计酬）→ 调 `platform_clearing` SQL 跨 schema 写入分账记录。
- 场景 002（出院）→ 调 `platform_clearing` + `platform_iot` 体征。
- 场景间协作走平台服务或 HTTP 接口（经网关 `/api/*`），严禁直接 import 跨场景代码。
- 共享类型 → `packages/shared-types`；共享组件 → `packages/ui`；AI 能力 → `platform-ai`。
