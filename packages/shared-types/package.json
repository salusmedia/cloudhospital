{
  "name": "@hospital/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "前后端共享的类型/契约。手写通用类型在 src/common.ts；各场景接口类型由 pnpm run gen:types 生成到 src/generated/。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@hospital/config": "workspace:*",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
