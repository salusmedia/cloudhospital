{
  "name": "@hospital/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "前端调后端的统一 SDK：基于 fetch，自动带鉴权、统一错误处理、走网关。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@hospital/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@hospital/config": "workspace:*",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
